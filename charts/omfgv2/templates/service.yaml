---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "omfgv2.fullname" . }}
  labels:
    {{- include "omfgv2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omfgv2.selectorLabels" . | nindent 4 }}
